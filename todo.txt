get mongo dump:
    docker exec mongo mongodump --out /data/dump
    docker cp mongo:/data/dump ./local_dump

    ...then it can be copied into the next image
    
convert back to alpine (when droplet comes back up) 
build image, copying mongodump
push to dockerhub

restore mongodb with dump
